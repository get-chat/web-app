import React from 'react';
import useContactList from '@src/components/ContactList/useContactList';

interface Props {}

const ContactList: React.FC<Props> = () => {
	const {} = useContactList();
};

export default ContactList;
